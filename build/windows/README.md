Place the real app icon here as `icon.ico` (multi-resolution, 16-256px)
before building. It is embedded directly into the binary for the system
tray icon, and `wails build` also picks it up from this directory to set
the compiled .exe's own icon.
